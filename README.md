# goto null;

An untitled website.

## Development

Setup:

```sh
brew install ruby
gem install bundler
mkdir -p vendor/bundler
bundle install --path vendor/bundle
```

Run:

```sh
bundle exec jekyll serve
```
